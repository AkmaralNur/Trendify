<resources>
    <!-- Базовая тема приложения. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Настройте вашу тему здесь. -->
        <item name="colorPrimary">@color/deep_water</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/ocean</item>
    </style>

    <style name="TestAppTheme" parent="Theme.AppCompat.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">#6a1b9a</item>
        <item name="colorPrimaryDark">#ec407a</item>
        <item name="colorAccent">#f44336</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="shoppingCartDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="myDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:backgroundDimEnabled">false</item><!-- активность не затемняется -->
    </style>

    <style name="PayDialog" parent="android:Theme.Holo.DialogWhenLarge">
        <!-- Цвет фона и степень прозрачности -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- Полупрозрачный фон -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- Нет заголовка -->
        <item name="android:windowNoTitle">true</item>
        <!-- Окно плавающее над активностью -->
        <item name="android:windowIsFloating">true</item>
        <!-- Нет затемнения фона -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- Установка прозрачности затемнения -->
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

</resources>
